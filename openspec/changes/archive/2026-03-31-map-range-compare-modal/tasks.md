## 1. 现状梳理与数据对齐

- [x] 1.1 通读 `src/views/WeaponDataView.vue`，定位右侧“武器/数据表格”的渲染位置与数据来源（列定义、行数据结构、格式化函数）
- [x] 1.2 列出“表格可见的全部字段/列”的稳定顺序与展示标签来源（以 `WeaponDataView` 的表头顺序 + `src/locales/*` 的 `weaponData.col*` 为准）
- [x] 1.3 确认每个字段是否可用于数值比较：对比逻辑基于 display 文案解析数值；无法解析/缺失时视为不可比较（不高亮）

## 2. 对比选择列（+ / -）与锁定规则

- [x] 2.1 在 `WeaponDataView` 增加对比选择状态：`compareSelectedIds: string[]`（长度 0-2）与便捷 computed（如 `isSelected(id)`、`compareLocked`）
- [x] 2.2 在表格最左侧新增「对比」列：未选显示 `+`，已选显示 `-`
- [x] 2.3 实现选择/取消选择逻辑：点击 `+` 加入、点击 `-` 移出；最多 2 个
- [x] 2.4 实现“选满 2 个时禁止选择其它武器”：未选中的 `+` 控件置为 disabled（仍允许对已选两行点击 `-`）
- [x] 2.5 为 disabled 状态补齐可访问性属性（`aria-disabled` / `disabled`）与样式（灰化、禁用光标）

## 3. 对比模态窗口：打开/关闭与三列布局

- [x] 3.1 新增 `compareModalOpen`：当 `compareSelectedIds.length === 2` 自动打开
- [x] 3.2 关闭模态窗口时不清空选择（保持锁定规则仍生效）
- [x] 3.3 实现模态窗口组件（在 view 内实现最小可用模态：遮罩、点击遮罩关闭、关闭按钮）
- [x] 3.4 在模态中渲染三列布局：左=武器 A，中=属性名，右=武器 B（标题/武器名清晰可识别）

## 4. 全字段对比与高亮规则（绿/红/中性）

- [x] 4.1 基于“表格列定义”生成对比行（覆盖表格可见的全部字段/列且顺序一致）
- [x] 4.2 为可比较字段定义 `direction`（higher/lower/na），并实现统一比较函数（基于 display 文案解析数值）
- [x] 4.3 当两侧均为有限数值且可判定优劣时：较优侧绿色背景、较差侧红色背景；相等/不可比为中性
- [x] 4.4 缺失/不可比较值保持 `—` / 原文案展示，且不高亮

## 5. 文案、样式与回归检查

- [x] 5.1 增加 i18n：表头「对比」、模态「属性」与关闭按钮到 `src/locales/zh.json` 与 `src/locales/en.json`
- [x] 5.2 增加样式：对比高亮（better/worse）与禁用态按钮样式
- [x] 5.3 验证交互：选中 2 个自动弹窗；关闭后仍不可选择其它；取消一个后恢复可选；`npm run build` 通过

