## 1. 状态与公式输入

- [x] 1.1 在 `useRadarMap`（或集中管理测距距离的 composable）中统一「当前公式距离 \(d\)」：无圆心、进入页、右键清除后 \(d = 0\)；有圆心未固定时按指针更新；固定后保持至清除或换图。
- [x] 1.2 确保右侧伤害计算、爆炸后状态、与距离相关的导出/派生值均读取上述 \(d\)，不得在 UI 层对「无测距」与「零距离」使用不一致的分支。

## 2. 地图 HUD 与右侧布局

- [x] 2.1 在地图正方形展示容器内增加右下角绝对定位叠加层，展示格式为「游戏单位数值 + 单位文案 + ` (xx.xx m)`」，样式复用左下角倍率 HUD 的类名或 token。
- [x] 2.2 从右侧信息栏移除原「当前距离」独占行；将「基本伤害」「计算伤害」行上移至紧接地图选择框之后（第二、三行），与 `design.md` / spec 一致。
- [x] 2.3 自测：未选点、拖动测距、固定圆、右键清除、切换地图等路径下，右下角文案与右侧计算是否与 spec 一致。

## 3. 规范与收尾

- [x] 3.1 实现完成后将本变更的 delta spec 按团队流程归档合并至 `openspec/specs/map-range-viewer/spec.md`（若在仓库内采用归档步骤则随 `openspec-archive` 处理）。
