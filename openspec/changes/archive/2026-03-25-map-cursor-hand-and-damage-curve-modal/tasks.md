## 任务拆解

- [x] 在 `public/map-range.html` 右侧新增按钮 `显示伤害曲线`，并实现模态窗口（Canvas + Tooltip + 关闭逻辑）。
- [x] 在模态窗口的 Canvas 上绘制两条曲线：无护甲伤害与有护甲（无护甲 × 0.5），并支持曲线附近悬浮显示 `(x, y)`。
- [x] 在模态窗口中点击曲线首次命中的点后，使用 `2px` 半径红点标记该位置，并保持不再更新。
- [x] 在地图视口实现左键长按光标切换为手型/抓手，并在松开/右键清除时恢复原光标；同时从 `public/map/<map>.txt` 解析 `pos_x/pos_y` 并在地图左侧/上侧绘制主/副刻度与坐标（主刻度 `1/2/5 * 10^n`，副刻度数量按 1/5 开头为 4、2 开头为 3）。

