## 1. 右侧参数与结果UI

- [x] 1.1 在 `public/map-range.html` 的“计算伤害”下方新增两个滑块：`初始HP`、`初始护甲`，均为 0-100、步长 1
- [x] 1.2 在滑块下方新增结果行，格式为 `HP:XX.XX (XX)，护甲：XX`；其中 HP 括号内显示向上取整值，护甲显示整数
- [x] 1.3 结果行增加颜色状态：HP<0 时红色并固定显示 `HP:0(0)，护甲：0`；否则黑色显示实际值

## 2. 结算计算逻辑

- [x] 2.1 实现护甲充足分支：当 `armor > damage * 0.5`，护甲减少 `floor(damage * 0.5)`，HP 减少 `damage * 0.5`
- [x] 2.2 实现护甲不足分支：当 `armor <= damage * 0.5`，护甲归零，HP 扣减为 `damage - armor`
- [x] 2.3 将伤害结果与 HP/护甲结算绑定到测距更新、固定圆状态和地图切换重置流程

## 3. 红圈规则重构

- [x] 3.1 引入基于滑块状态的红圈目标阈值：`HP >= armor` 时 `T = HP + armor`，否则 `T = HP * 2`
- [x] 3.2 按当前伤害公式（含 `/2`）对目标阈值 `T` 反解红圈距离并绘制；不可达阈值时采用不绘制或安全降级
- [x] 3.3 保持绿圈（`baseDamage * 3.5`）与测距圆逻辑不变，确认叠层绘制顺序与刷新时机正确

## 4. 验证

- [x] 4.1 手测滑块边界（0/100）与步长（1），确认结果行格式、取整与颜色规则符合需求
- [x] 4.2 构造护甲充足/不足两类样例，核对 HP/护甲扣减值与红圈阈值切换是否正确
