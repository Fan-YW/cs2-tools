## 1. 计算规则调整

- [x] 1.1 在伤害计算入口增加超距判断：当 `distance > range` 时直接返回各列 0 值结果
- [x] 1.2 将无护甲四列计算统一改为“基础部位伤害 × distance 衰减 × bullets”
- [x] 1.3 将有护甲三列计算统一改为“基础部位伤害 × distance 衰减 × WeaponArmor/2 × bullets”
- [x] 1.4 补齐 `bullets` / `range` 缺失或无效时的安全兜底，避免出现 `NaN`

## 2. 表格展示与样式

- [x] 2.1 在伤害表渲染层增加阈值样式逻辑：展示值 `>= 100` 时应用红色文本样式
- [x] 2.2 确保超距时无护甲列展示为 `0.00`，有护甲列展示为 `0.00(0)`
- [x] 2.3 校验高亮逻辑与格式化顺序，避免出现显示值与高亮状态不一致

## 3. 验证与回归

- [x] 3.1 增加/更新计算测试：覆盖 `distance == range`、`distance > range`、`bullets > 1`
- [x] 3.2 增加/更新渲染测试：覆盖阈值边界 `99.99` 与 `100.00` 的颜色断点
- [ ] 3.3 手动回归地图测距联动，确认距离变化会实时驱动新规则并正确更新表格
