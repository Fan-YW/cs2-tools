## 1. 页面骨架与 Pico CSS

- [x] 1.1 在 `public/` 下新增单页 HTML（如 `map-range.html`），通过 CDN 引入 Pico CSS，使用原生语义标签搭建左右分栏布局
- [x] 1.2 左侧容器设为「高度约 100vh、宽度等于高度」的正方形地图视口；右侧放置 `<select>` 与四行信息区（地图名下方依次为距离、基本伤害、计算伤害的标签与占位）

## 2. 地图资源与元数据（txt → json）

- [x] 2.0 **优先**：在 `public` 下创建 **`json/`** 与 **`png/`** 目录；编写脚本或一次性流程，将源 `.txt`（如 `public/map/*.txt`）转为 **`public/json/<基名>.json`**（至少含 `scale`、`c4_base_damage`）；将 7 张雷达图归档至 **`public/png/`**（保持 `*_radar_psd.png` 命名）；实现阶段不得仅以未转换的 txt 作为唯一数据源
- [x] 2.1 列出 7 张地图基名，填充下拉选项；`<img>` 的 `src` 指向 **`/png/<name>_radar_psd.png`**（或等价相对路径）
- [x] 2.2 实现 `fetch` 从 **`/json/<name>.json`** 加载元数据，解析 `scale` 与 `c4_base_damage`（失败时显示明确错误状态）

## 3. 缩放、平移与倍率

- [x] 2.3 实现地图**铺满**正方形视口：`cover` 语义或等价最小缩放，使 1024×1024 图层始终覆盖视口；实现**平移夹紧**，保证任意缩放/拖动后**不出现白边或未覆盖区域**
- [x] 2.3b 在地图区域实现**左键按住拖动**平移；与测距用**拖动阈值**区分：未超阈值且在 `mouseup` 计为**单击**落点，超阈值则计为平移、不记录该次单击圆心
- [x] 2.4 在地图区域 `pointer` 悬停时监听 `wheel`，在「铺满视口的最小缩放」与「\(X=1\)」之间钳制缩放；缩放后必要时修正平移以防露白；同步更新图像变换与倍率文案（在 `design.md` 中选定缩放锚点并实现）

## 4. 测距圆、参考圆与伤害

- [x] 3.1 使用 Canvas 或 SVG 覆盖层，与 `<img>` 的缩放/平移保持一致，绘制「圆心 + 半径至指针」的测距圆
- [x] 3.1b 第一次左键落点后，在同一圆心绘制**红圈**（半径为 \(d_{100}=\text{base}\cdot\sqrt{\ln(100/\text{base})\cdot(-2\cdot7^2/6^2)}\)，仅当 `baseDamage >= 100`）与**绿圈**（半径 `baseDamage * 3.5` 游戏单位）；将游戏单位半径除以当前倍率 \(X\) 得到屏幕像素半径
- [x] 3.2 **单击**落点记录圆心；在**未按住左键**时移动鼠标更新测距圆半径、像素距离、游戏单位距离（像素距离 × \(X\)），并更新第二行距离与第四行伤害：若距离 \(> 3.5 \times \text{baseDamage}\) 则显示 **0**，否则按指数公式（使用当前 `c4_base_damage`）
- [x] 3.2a 距离行在游戏单位后**括号**内展示米制距离，**1 unit = 25.4 mm**（即米 = 游戏单位 × 0.0254）
- [x] 3.3 第二次左键固定测距圆与数值，停止随鼠标更新；红/绿参考圆保持显示；右键 `preventDefault` 并清除测距圆、参考圆与圆心状态
- [x] 3.4 切换地图时重置缩放/测距状态或按规范重新加载元数据

## 5. 验证

- [x] 4.1 用 `de_dust2`、展示框边长 512px、`scale` 4.4 手动核对倍率是否为 8.8；在距离略小于/大于 `3.5 * baseDamage` 时核对伤害为公式值与 **0**
- [x] 4.2 在主流 Chromium 浏览器中自测滚轮缩放上下界、左键/右键交互与布局在常见视口高度下的表现
